import React, { useState } from "react";
import { Typography } from '@mui/material';

function Tetris(props) {
  return (
    <Typography variant="h4" gutterBottom>Tetris Game</Typography>
  );
}
    
export default Tetris;